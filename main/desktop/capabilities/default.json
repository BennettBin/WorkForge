{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for local desktop development.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
