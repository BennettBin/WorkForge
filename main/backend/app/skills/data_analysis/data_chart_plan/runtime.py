from __future__ import annotations


def run(payload: dict) -> dict:
    return {
        "charts": ["Trend line chart", "Category bar chart", "Distribution histogram"],
        "notes": "Select charts based on variable types.",
    }

