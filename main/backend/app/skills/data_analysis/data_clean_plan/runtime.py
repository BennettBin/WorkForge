from __future__ import annotations


def run(payload: dict) -> dict:
    return {
        "steps": [
            "Schema scan",
            "Missing-value strategy",
            "Type normalization",
            "Outlier handling",
            "Validation checks",
        ]
    }

